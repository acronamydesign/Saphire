var _ = require('underscore');

var prepare = {
	appendData:function(dataStore, newData){
		_.extend(dataStore, newData);
	},
	purposeSelector:function(name){
		return this[name]["data"];
	}
}

module.exports = prepare;
