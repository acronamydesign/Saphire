var fs = require('fs'),
		map = require('../src.map.js');

activeTheme = function(publicRoot){
	var themesPath = map.deployTheme+"/"+publicRoot+"/themes",
				themes = fs.readdirSync(themesPath);
		return themes;
}

module.exports = activeTheme;
