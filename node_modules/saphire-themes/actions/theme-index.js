recursive = require("recursive-readdir-sync")

module.exports = function(currentTheme){
	try {
			return recursive(currentTheme);
		} catch (err) {
			if (err.errno === 34) {
				console.log('theme does not exist');
			} else {
				console.log("Something broke, :(")
				throw err;
			}
		}
}
