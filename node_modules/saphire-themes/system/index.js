var jade = require('jade');

//this is our interpreter we can catch stuff here?

//allow overides from themes folder... sweet!
var variableOutput = function (head, body, currentTheme) {

	var systemTemplateDir = __dirname + "/templates/";
	console.log(systemTemplateDir)

	// Compile a function
	var head = jade.compileFile(systemTemplateDir + "html.tpl.jade");
	var body = jade.compileFile(systemTemplateDir + "page.tpl.jade");

	// Render the function
	var head = head( /*takes assets object*/ );
	var body = body();
	console.log(body)
	//ADD SCRIPTS AT END
	var compile = {
		head: head,
		body: body
	}



	var compile = "<html lang='FROM DB'>" + head + body + "</html>";

	module.exports = {
		compile: compile,
		variableOutput: variableOutput
	};

}
