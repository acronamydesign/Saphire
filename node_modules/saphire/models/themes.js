var mongoose = require("mongoose"),
		schema = mongoose.Schema

var themesSchema = new schema({
	id:Number,
	machineName:String,
	freindlyName:String,
	state:Boolean,
	admin:Boolean
});

mongoose.model("plugin",themesSchema)
