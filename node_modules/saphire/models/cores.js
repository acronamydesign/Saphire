var mongoose = require("mongoose"),
		schema = mongoose.Schema

var coresSchema = new schema({
	name:String,
	version:String,
	description:String,
	repo:String
});

mongoose.model("cores",coresSchema)
