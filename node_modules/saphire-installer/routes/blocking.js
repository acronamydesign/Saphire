//during install we dont want to allow navigation to admin, convention dictates that all new modules should use admin/new module to allow this blocking, we tried redirect but it caused a lot of problems.

module.exports = function(app){
	app.get("/admin/*", function(req,res){
		res.redirect("/");
	});
}
