var saphireExpose = require("saphire").functions,
	mongoose = saphireExpose.db.mongoose,
	schema = mongoose.Schema;


var userSchema = new schema({
	fullname: String,
	username: String,
	password: String,
	su: Boolean
});

var users = mongoose.model("users", userSchema)

module.exports = users;
